services:
  - type: web
    name: pub-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --bind 0.0.0.0:$PORT --timeout 60
    envVars:
      - key: API_TOKEN
        sync: false